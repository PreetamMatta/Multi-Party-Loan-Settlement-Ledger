# Entity Relationship Diagram

ERD added after schema is finalized.

The current source of truth for table structure is [`backend/db/schema.sql`](../backend/db/schema.sql).
Once the schema stabilizes (post-Session 3), generate a Mermaid or dbdiagram.io ERD here.

**Update this file when:** any structural change lands via a migration in `backend/db/migrations/`.
